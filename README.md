steam-linkfilter-disabler-addon
===============================

Really simple addon for chrome/firefox that disables the linkfilter page when visiting urls

Packaged addons are in the `release` folder

`.xpi` is for Firefox
`.crx` is for Chrome
